# JSON Contracts

Machine-facing schemas live in:
- `docs/contracts/README.md`
- `docs/contracts/*.schema.json`

When changing JSON shapes in `src/domain/models.rs`, update contract docs/schemas in the same change.
