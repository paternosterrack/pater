# Lifecycle Commands

Common command path:

```bash
pater search <query>
pater show <plugin@market>
pater install <plugin@market>
pater list
pater update [plugin]
pater remove <plugin>
```

Capability-focused path:

```bash
pater ensure --intent "typescript" --agent codex
pater apply <plugin@market> --target-adapter codex
```
